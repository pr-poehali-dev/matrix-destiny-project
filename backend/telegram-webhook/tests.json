{
  "tests": [
    {
      "name": "OPTIONS request returns CORS",
      "method": "OPTIONS",
      "expectedStatus": 200,
      "expectedHeaders": {
        "Access-Control-Allow-Origin": "*"
      }
    }
  ]
}
